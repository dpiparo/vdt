// Automatically generated
#include "vdtMath.h"
namespace vdt{
void acosv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=acos(iarray[i]);
}

void asinv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=asin(iarray[i]);
}

void atanv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=atan(iarray[i]);
}

void cosv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=cos(iarray[i]);
}

void expv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=exp(iarray[i]);
}

void fast_acosv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_acos(iarray[i]);
}

void fast_approx_invv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_approx_inv(iarray[i]);
}

void fast_approx_isqrtv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_approx_isqrt(iarray[i]);
}

void fast_asinv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_asin(iarray[i]);
}

void fast_atanv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_atan(iarray[i]);
}

void fast_cosv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_cos(iarray[i]);
}

void fast_expv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_exp(iarray[i]);
}

void fast_invv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_inv(iarray[i]);
}

void fast_isqrtv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_isqrt(iarray[i]);
}

void fast_logv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_log(iarray[i]);
}

void fast_sinv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_sin(iarray[i]);
}

void fast_tanv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_tan(iarray[i]);
}

void identityv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=identity(iarray[i]);
}

void invv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=inv(iarray[i]);
}

void isqrtv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=isqrt(iarray[i]);
}

void logv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=log(iarray[i]);
}

void sinv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=sin(iarray[i]);
}

void tanv(const uint32_t size, double* __restrict__ iarray, double* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=tan(iarray[i]);
}

void acosfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=acosf(iarray[i]);
}

void asinfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=asinf(iarray[i]);
}

void atanfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=atanf(iarray[i]);
}

void cosfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=cosf(iarray[i]);
}

void expfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=expf(iarray[i]);
}

void fast_acosfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_acosf(iarray[i]);
}

void fast_approx_invfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_approx_invf(iarray[i]);
}

void fast_approx_isqrtfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_approx_isqrtf(iarray[i]);
}

void fast_asinfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_asinf(iarray[i]);
}

void fast_atanfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_atanf(iarray[i]);
}

void fast_cosfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_cosf(iarray[i]);
}

void fast_expfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_expf(iarray[i]);
}

void fast_invfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_invf(iarray[i]);
}

void fast_isqrtfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_isqrtf(iarray[i]);
}

void fast_logfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_logf(iarray[i]);
}

void fast_sinfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_sinf(iarray[i]);
}

void fast_tanfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=fast_tanf(iarray[i]);
}

void identityfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=identityf(iarray[i]);
}

void invfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=invf(iarray[i]);
}

void isqrtfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=isqrtf(iarray[i]);
}

void logfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=logf(iarray[i]);
}

void sinfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=sinf(iarray[i]);
}

void tanfv(const uint32_t size, float* __restrict__ iarray, float* __restrict__ oarray){
  for (uint32_t i=0;i<size;++i)
    oarray[i]=tanf(iarray[i]);
}

} // end of vdt namespace